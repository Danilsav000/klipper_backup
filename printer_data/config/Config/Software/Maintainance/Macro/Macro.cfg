
[gcode_macro UPDATE_MAINTAINS_LUBRICATION]
description: Reset timer for lubrication maintains
gcode:
  UPDATE_MAINTENANCE NAME=lubricate_rails

[gcode_macro UPDATE_MAINTAINS_AIRFILTERS]
description: Reset timer for airfilters maintains
gcode:
  UPDATE_MAINTENANCE NAME=airfilters

[gcode_macro UPDATE_MAINTAINS_SCREWS]
description: Reset timer for screws maintains
gcode:
  UPDATE_MAINTENANCE NAME=screws

[gcode_macro MAINTAINS_STATUS]
description: Get maintains status
gcode:
  MAINTAIN_STATUS
